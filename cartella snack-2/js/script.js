const arrayName = ['Leo','Leandro','Lucia','Piero','Luca'];

const arrayLastName = ['Lidio','Lidia','Lippolis','Lillo','Lerte'];

let arrayGuestName = [];

for ( i=0; i=arrayName.length && arrayLastName.length i=i+1 ) {
    let arrayGuestName = arrayName.length[i] + arrayLastName.length[i];
    console.log(arrayGuestName);
}